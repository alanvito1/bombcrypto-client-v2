# System Atlas: Bombcrypto Game Client

**Date**: 2023-10-27
**Scope**: Unity Client (`Assets/`)
**Status**: Verified

## 1. Architectural Overview
This repository contains the Unity-based Game Client for Bombcrypto. It follows a Service-Oriented Architecture (SOA) within Unity, utilizing a `ServiceLocator` pattern (inferred) to manage dependencies like `IApiManager`. The client communicates with a remote backend via HTTP/REST endpoints.

## 2. Inventory Stats
- **Languages**: C# (Unity), HLSL (Shaders - inferred)
- **Core Framework**: Unity Engine 2022.3 (per README)
- **Key Libraries**:
  - `Newtonsoft.Json` (JSON Parsing)
  - `DOTween` (Animation)
  - `UniTask` (Async/Await)
  - `CodeStage.AntiCheat` (Security)
  - `Sfs2X` (SmartFoxServer - inferred from imports)

## 3. API Inventory (Detected Endpoints)
*Source: `Assets/Scripts/Services/DefaultApiManager.cs`*

| Method | Endpoint Path | Description |
| :--- | :--- | :--- |
| `GET` | `coin_balance?address={address}` | Retrieves BCoin balance for a wallet. |
| `GET` | `ccu` | Get Concurrent Users (CCU) and Max CCU. |
| `GET` | `pvp-matching-2/tournament/room/status` | List active PvP tournament rooms. |
| `GET` | `pvp-matching-2/tournament/status` | List PvP match schedules. |
| `POST` | `pvp-matching-2/tournament/my-matches` | Get match history for a specific user. |

## 4. Component Inventory (Key Classes)

### Core Engine (`Assets/Scripts/Engine/`)
| Class | Responsibility | Key Properties |
| :--- | :--- | :--- |
| `Entity` | Base class for game objects. | `IsAlive`, `Type`, `EntityManager`, `Index` |
| `EntityLocation` | Entity with spatial hash. | `HashLocation` |

### Services (`Assets/Scripts/Services/`)
| Class | Responsibility | Key Methods |
| :--- | :--- | :--- |
| `DefaultApiManager` | Handles HTTP REST communication. | `GetCoinBalance`, `GetPvpRoomList` |
| `Utils` | Static helper functions. | `GetWebResponse`, `FormatBcoinValue`, `LoadImageFromUrl` |
| `TimeUtil` | Time formatting helpers. | `ConvertTimeToString`, `ConvertTimeToStringFull` |

## 5. Data Models (Inferred from usage)
*Source: `Assets/Scripts/Services/DefaultApiManager.cs`*

- **PvpRoomInfo**: Represents a PvP room (deserialized from `pvp-matching-2/tournament/room/status`).
- **PvpMatchSchedule**: Represents a scheduled match (deserialized from `pvp-matching-2/tournament/status`).
- **IPvpRoomInfo**: Interface for room info.
- **IPvpMatchSchedule**: Interface for match schedule.

## 6. Scribe's Journal (Gap Analysis)

### ⚠️ Ambiguities
1.  **Backend Location**: The backend code is not present in this repository, despite documentation suggesting a `Server/` directory.
2.  **Service Locator**: Usage of `ServiceLocator` is implied but not fully visible in the scanned files.
3.  **Authentication**: `DefaultApiManager` does not visibly handle Auth Tokens in headers for `GetCoinBalance`, implying it might be a public endpoint or handled by `Utils.GetWebResponse` implicitly (though `Utils` code shows generic header injection).

### 🛡️ Security Risks
1.  **Hardcoded Configurations**: `DefaultApiManager` has references to `ApiHost` but logic seems to rely on `AppConfig` which is good.
2.  **Logging**: `Utils.GetWebResponse` logs the full URL and Response body (`logManager.Log($"result = {res}")`). This could leak sensitive data if the response contains PII or tokens.
3.  **Exception Handling**: `GetCoinBalance` throws a generic `Exception(message)` if the API returns an error code, which might make specific error handling difficult upstream.

### 📝 Missing Validation
1.  **Input Sanitization**: `GetCoinBalance` accepts a `walletAddress` string but does not validate its format before making the network request.
2.  **Null Checks**: `DefaultApiManager.GetPvpRoomList` assumes `msg["details"]` is a `JArray`. If it's null, it might throw a runtime exception.

---
*Generated by Deep Scribe*
